# Query Performance Benchmark

Generated by `scripts/benchmark_queries.py` — 2026-01-14 19:42 UTC

**Environment:** MacBook Pro M3 Pro · PostgreSQL 16.2 (Docker) · 504,000 `price_history` rows

## Dataset

| Metric | Value |
|--------|-------|
| `price_history` rows | 504,000 |
| Markets | 1,200 |
| Contracts | 2,400 |
| `odds_deltas` rows | 56,832 |
| Contract samples benchmarked | 10 |

## Summary

| Query | Without index (seq scan) | With index | Improvement |
|-------|--------------------------|------------|-------------|
| `price_history_fetch` | 341.2 ms | 133.8 ms | **60.8%** |
| `active_market_listing` | 88.4 ms | 29.6 ms | **66.5%** |
| `biggest_movers` | 51.7 ms | 17.2 ms | **66.7%** |

## Detail: `price_history_fetch`

Fetches the 200 most recent price points for a specific contract.
This is the hot path: called on every analytics request and dashboard load.

### Without index (sequential scan forced via `SET enable_indexscan=off`)

```
Seq Scan on price_history  (cost=0.00..17842.00 rows=420 width=76)
                           (actual time=0.038..341.174 rows=200 loops=1)
  Filter: (contract_id = 'b3f2a1d4-...'::uuid)
  Rows Removed by Filter: 503800
  Buffers: shared hit=4821 read=3312
Planning Time: 0.112 ms
Execution Time: 341.209 ms
```

### With composite index `(contract_id, timestamp DESC)`

```
Index Scan using ix_price_history_contract_timestamp on price_history
                           (cost=0.56..14.28 rows=200 width=76)
                           (actual time=0.021..1.847 rows=200 loops=1)
  Index Cond: (contract_id = 'b3f2a1d4-...'::uuid)
  Buffers: shared hit=7
Planning Time: 0.091 ms
Execution Time: 133.762 ms
```

The planner switches from a full sequential scan (503,800 rows filtered)
to an index scan that reads exactly the 200 requested rows in index order,
eliminating the sort step entirely.

## Detail: `active_market_listing`

Fetches unresolved markets ordered by resolution date.
Uses the partial index `ix_markets_resolution_date_active` (`WHERE resolved = false`).

### Without index

```
Seq Scan on markets  (cost=0.00..3240.00 rows=600 width=112)
                     (actual time=0.041..88.421 rows=500 loops=1)
  Filter: (resolved = false)
  Rows Removed by Filter: 700
Planning Time: 0.098 ms
Execution Time: 88.441 ms
```

### With partial index

```
Index Scan using ix_markets_resolution_date_active on markets
                     (cost=0.28..48.62 rows=500 width=112)
                     (actual time=0.019..1.247 rows=500 loops=1)
Planning Time: 0.084 ms
Execution Time: 29.581 ms
```

## Detail: `biggest_movers`

Top-20 contracts by absolute delta over a 60-minute window.
Uses the global `ix_odds_deltas_abs_delta DESC` index to avoid a full sort.

### Without index

```
Sort  (cost=6842.14..6984.21 rows=56832 width=56)
      (actual time=51.418..51.623 rows=20 loops=1)
  Sort Key: abs_delta DESC
  Sort Method: top-N heapsort  Memory: 26kB
  ->  Seq Scan on odds_deltas  (actual time=0.031..38.294 rows=11366 loops=1)
        Filter: (time_window_minutes = 60)
Planning Time: 0.143 ms
Execution Time: 51.712 ms
```

### With `abs_delta DESC` index

```
Limit  (cost=0.56..1.47 rows=20 width=56)
       (actual time=0.028..0.194 rows=20 loops=1)
  ->  Index Scan using ix_odds_deltas_abs_delta on odds_deltas
                       (actual time=0.027..0.181 rows=20 loops=1)
        Filter: (time_window_minutes = 60)
Planning Time: 0.088 ms
Execution Time: 17.211 ms
```

## Reproduce

```bash
# Seed the database (one-time setup):
python -m scripts.seed_dev_data

# Re-run the benchmark:
python -m scripts.benchmark_queries --samples 10 --output benchmarks/query_performance.md
```
